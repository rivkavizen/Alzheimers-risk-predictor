# Supabase — project dashboard:
# https://supabase.com/dashboard/project/puhtwpurgkqylnanortj
# API keys: Settings → API → service_role (secret)
SUPABASE_URL=https://puhtwpurgkqylnanortj.supabase.co
SUPABASE_KEY=your-service-role-key

# Model paths (relative to project root — same values on Railway)
MODEL_PATH=ml/model_artifacts/model.pkl
FEATURE_NAMES_PATH=ml/model_artifacts/feature_names.json
SHAP_FEATURE_NAMES_PATH=ml/model_artifacts/shap_feature_names.json

# Optional server default Claude model (visitors still use their own API keys)
# ANTHROPIC_MODEL=claude-sonnet-4-6

# Railway: add SUPABASE_URL, SUPABASE_KEY, MODEL_PATH, FEATURE_NAMES_PATH as project Variables.
# Do NOT add CLAUDE_API_KEY — visitors provide their own key in the browser.
